<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.muhammadfaisal.formulirapp">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".FormulirApplication"
        android:theme="@style/Theme.FormulirApp">
        <activity
            android:name=".activity.ExportActivity"
            android:exported="false" />
        <activity
            android:name=".activity.NpwpFormActivity"
            android:exported="false" />
        <activity
            android:name=".activity.SimFormActivity"
            android:exported="false" />
        <activity
            android:name=".activity.TakePictureActivity"
            android:exported="false" />
        <activity
            android:name=".activity.KtpFormActivity"
            android:exported="false" />
        <activity
            android:name=".activity.FormActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>